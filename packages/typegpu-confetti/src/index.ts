export {
  // slots
  canvasAspectRatio,
  particles,
  maxDurationTime,
  initParticle,
  maxParticleAmount,
  deltaTime,
  time,
  gravity,
  // function shells
  type GravityFn,
  gravityFn,
  type InitParticleFn,
  initParticleFn,
} from './confetti-schemas';

export type { ConfettiPropTypes, ConfettiRef } from './types';
