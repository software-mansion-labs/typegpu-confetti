export {
  // slots
  canvasAspectRatio,
  particles,
  maxDurationTime,
  initParticle,
  maxParticleAmount,
  deltaTime,
  time,
  gravity,
  // function shells
  gravityFn,
  initParticleFn,
} from './confetti-schemas';

export type { ConfettiPropTypes, ConfettiRef } from './types';
