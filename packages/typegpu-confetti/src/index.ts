export {
  // slots
  canvasAspectRatio,
  deltaTime,
  // function shells
  type GravityFn,
  gravity,
  gravityFn,
  type InitParticleFn,
  initParticle,
  initParticleFn,
  maxDurationTime,
  maxParticleAmount,
  particles,
  time,
} from './schemas';

export type { ConfettiPropTypes, ConfettiRef } from './types';
